apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cert-manager-acm-importer.fullname" . }}-test-connection"
  labels:
    {{- include "cert-manager-acm-importer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cert-manager-acm-importer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
